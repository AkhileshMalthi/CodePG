# Copy this file to .env and fill in local overrides if needed
# No API keys required (AI feature removed)

# Optional overrides (also settable via config file or CODEPG_* env vars)
# CODEPG_BASE_DIR=~/CodePG
# CODEPG_EDITOR_COMMAND=code "{path}"
